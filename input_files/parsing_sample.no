{
    var x = 1;
    var y = 20 + 44 * x;

    var s = 'string value';
    var bool_var = true;

    x = y + 1;

    while(x<4) {
        if (x > 1) {
            x = x + 1;
        };
    };

    print y + 5;
}